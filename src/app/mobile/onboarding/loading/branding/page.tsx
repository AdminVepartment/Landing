import { DeptLoadingScreen } from "../_components/dept-loading-screen";

export default function BrandingLoadingPage() {
  return <DeptLoadingScreen dept={{ name: "Branding", colorVar: "branding" }} />;
}
