import { DeptLoadingScreen } from "../_components/dept-loading-screen";

export default function SustainabilityLoadingPage() {
  return <DeptLoadingScreen dept={{ name: "Sustainability", colorVar: "sustainability" }} />;
}
